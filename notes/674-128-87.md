# [[674,128,87]] designed-divisor generalized-bicycle CSS code

This lift-337 generalized-bicycle CSS code has check-matrix ranks 273 per
side, encodes 128 logical qubits, and has maximum check weight 30. Its
circulant supports are

```text
a(x) = 1 + x + x^12 + x^24 + x^27 + x^69 + x^93 + x^113
       + x^128 + x^143 + x^149 + x^162 + x^262 + x^269
       + x^294 + x^309
b(x) = x^17 + x^36 + x^38 + x^50 + x^64 + x^79 + x^81
       + x^82 + x^83 + x^144 + x^145 + x^247 + x^291
       + x^325
```

The designed-divisor search fixed the encoded dimension and screened candidate
support pairs with progressively larger information-set searches. The
100,000-, 500,000-, and 2,000,000-trial stages produced global upper bounds
88, 90, and 88. A fresh 8,000,000-trial depth-8 RIS-fast run per CSS
direction, using 18 threads and seed 1470671, found independently validated
weight-88 X and weight-87 Z logical operators.

The included witnesses therefore support only the upper-bound claim `d<=87`;
no distance lower bound or exact-distance claim is made. The resulting reported
score is

```text
k d^2 / n = 128 * 87^2 / 674 = 1437.4362017804153.
```
