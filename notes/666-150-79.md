# [[666,150,79]] designed-divisor generalized-bicycle CSS code

This lift-333 generalized-bicycle CSS code has check-matrix ranks 258 per
side, encodes 150 logical qubits, and has maximum check weight 30. Its
circulant supports are

```text
a(x) = 1 + x + x^17 + x^37 + x^58 + x^93 + x^107 + x^136
       + x^147 + x^149 + x^152 + x^165 + x^260 + x^273
       + x^324 + x^326
b(x) = x^3 + x^73 + x^84 + x^91 + x^102 + x^145 + x^167
       + x^183 + x^192 + x^226 + x^278 + x^280 + x^298
       + x^327
```

The designed-divisor search fixed the encoded dimension and screened candidate
support pairs with progressively larger information-set searches. The
100,000-, 500,000-, and 2,000,000-trial stages produced global upper bounds
85, 83, and 83. A separate 5,000,000-trial depth-8 run per CSS direction,
using seed 859320321, found independently validated weight-82 X and Z logical
operators. The submission gate subsequently found a valid weight-79 Z logical
operator with RIS-fast (refutation seed 575260165), tightening the global
upper bound.

The included witnesses therefore support only the upper-bound claim `d<=79`;
no distance lower bound or exact-distance claim is made. The resulting reported
score is

```text
k d^2 / n = 150 * 79^2 / 666 = 1405.6306306306305.
```
